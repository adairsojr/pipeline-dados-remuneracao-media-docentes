name: remuneracao_docentes
profile: remuneracao_docentes
version: 1.0.0

model-paths:
  - models

models:
  remuneracao_docentes:
    silver:
      +materialized: external
      +format: parquet
    gold:
      +materialized: external
      +format: parquet

flags:
  require_generic_test_arguments_property: true
